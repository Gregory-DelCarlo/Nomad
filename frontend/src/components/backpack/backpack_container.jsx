import { connect } from 'react-redux';
import Backpack from './backpack';

// const mapStateToProps = state => ({
//   loggedIn: state.session.isAuthenticated
// });

export default connect(null, null)(Backpack);